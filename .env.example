# PostgreSQL configuration for local development
POSTGRES_DB=bes_catalogs
POSTGRES_USER=bes_app
POSTGRES_PASSWORD=bes_secret
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Optional unified connection string used by psycopg
DATABASE_URL=postgresql://bes_app:bes_secret@localhost:5432/bes_catalogs

# Catalog settings
CATALOG_SOURCE=db
# Override the default Excel path if needed
# CATALOG_EXCEL_PATH=coeficientes NOVOMET.xlsx
